# Cloudflare Pages redirects
# Trailing slash handling is managed by Next.js static export (trailingSlash: true)
# No explicit redirects needed — Cloudflare Pages serves /dir/index.html automatically
